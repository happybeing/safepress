'use strict';

describe('PostActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/PostActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
